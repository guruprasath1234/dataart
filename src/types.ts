// src/types.ts
export interface EventData {
  year: string;        // if your events.json has years as strings like "2000"
  title: string;
  description: string;
  imageURL: string;
  category: string;
}
